services:
  - type: static
    name: SmartCafe
    buildCommand: npm install && npm install --prefix SmartCafe && npm run build --prefix SmartCafe
    staticPublishPath: ./SmartCafe/build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
